#include<iostream>
using namespace std;
int main(){
    int a;
    if(3+4%7) cout<<"this works"<<endl;
    if(a=10) cout<<"this also works"<<endl;
    if(-8) cout<<"this works or not??"<<endl;

    // if(acts like boolean){
    //     this works if it is true
    // }
}